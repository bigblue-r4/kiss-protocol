# Contributing

Small focused pull requests are preferred.

Before submitting:

1. Run `gofmt -w` on changed Go files.
2. Run `go test ./...`.
3. Keep install and packaging changes documented.

Security-sensitive findings should be reported privately first. See `SECURITY.md`.
